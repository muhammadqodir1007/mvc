package com.epam.dao.repo.creator;

public enum FilterParameters {

    name,
    tag_name,
    partOfName,
    partOfDescription,
    sortByName,
    sortByCreateDate,
    sortByTagName,
    partOfTagName,
    description,
    create_date

}
